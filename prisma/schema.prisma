// This is your mongo schema file,
// learn more about it in the docs: https://pris.ly/d/mongo-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try mongo Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "mongo-client-js"
  output   = "../src/generated/mongo"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
